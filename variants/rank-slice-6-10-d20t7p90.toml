name = "rank-slice-6-10-d20t7p90"
question = "Ranks 6-10 on the d20-t7-p90 spine (the batch-2 width benchmark): does the ranking matter more when the entry machine recycles 5x faster?"
base = "frozen"
status = "rejected"
created = "2026-08-18"
updated = "2026-08-18"
touches = ["entry"]
verdict = """
OUTSIDE band; the clause that keeps selection open -- 2026-08-18, Analyzer, batch-3 grading (campaign pauses at the owner's gate; shortlist empty). Verbatim:

Autopsy decomposition: -13.8pp book lottery + -6.8pp starvation, with per-campaign realized $180 >= top-5's $164. The bar's letter fires the signal branch; the decomposition says the 'signal' is not name-ranking signal. Both recorded; resolved only by the Batch-4 controls.
"""
note = """
Pre-registration: vault note "10 Live Paper Bot/2026-08-18 -- Batch 3 pre-registration, selection and the last dials.md", bars written before any run. Declared the LAST batch before campaign-level certification unless a registered bar is cleared. Noise rules carried: A-1b (n5 deltas < 15pp = book noise) and A-5 (a candidate must survive removal of its largest open mark).

THE BAR (registered, H-B2-3/H2): if slice-6-10 lands within the 15pp book-noise band of slice-1-5 (the plain top-of-list base) on BOTH spines -> ranking carries no measurable signal (the C12 shape) -> the selection axis is certified inert and the campaign moves to certification. If slice-6-10 is materially worse -> selection has signal; preference/ranker arms on the width spine at n20 become a proposed Batch 4 WITH the owner sign-off -- not automatic.

This arm: the second spine. d20-t7-p90 turns the machine ~5x faster (237 campaigns vs 142 at n5), so a selection signal has ~5x the events to show itself here -- the strongest place for the C12 question to get a real answer.
"""

[ops]
# Batch-3 width: every n5 claim is stated at 5 slots, $20k/slot.
n_slots = 5

[knobs.rank_window]
value = [6, 10]
why = """
The measurement instrument, not a strategy: take entries from ranks 6-10 of the sorted pool instead of its top. A slice, never a veto -- it changes WHICH name fills the slot, not how many were eligible; re-applied to the re-ranked pool every fill. A pool shorter than 6 fills nothing, loudly (entry_rank_window_empty) -- the fallback searches only the slice, so the window cannot silently become top-of-list. Built and tested 2026-08-18 (test_rank_window.py, 6 cases).
"""
source = "Batch-3 pre-registration Wave 1 (2026-08-18)"

[knobs.put_delta]
value = 0.2
why = """
The width-benchmark spine's delta (batch-2 verdict: retained as Batch 3's second spine).
"""
source = "Batch-2 grid card spine-d20-t7-p90 (width benchmark verdict)"

[knobs.target_dte]
value = 7
why = """
The width-benchmark spine's tenor.
"""
source = "Batch-2 grid card spine-d20-t7-p90 (width benchmark verdict)"

[knobs.take_profit_pct]
value = 0.9
why = """
The width-benchmark spine's take-profit.
"""
source = "Batch-2 grid card spine-d20-t7-p90 (width benchmark verdict)"

