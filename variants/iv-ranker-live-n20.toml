name = "iv-ranker-live-n20"
question = "Same question as iv-ranker-live, second width (n20, 400k) -- both widths read together per the pre-registered bar."
base = "frozen"
status = "rejected"
created = "2026-08-22"
updated = "2026-08-22"
touches = ["ranking"]
verdict = """
REJECTED 2026-08-22 as the n20 half of the joint ship bar (see iv-ranker-live.toml; same engine/data/card pins). n20 (400k, 20 slots): +4.64% vs frozen n20 +0.19% vs SPY +37.34%. Sharpe 0.23 vs 0.09; maxDD -22.19% vs -22.23%; by_year -1.88/+3.80/+2.87 vs +3.76/-4.78/+1.10.

This half PASSED its own clause (within 8pp of frozen n20 -- in fact +4.45pp better, better in 2/3 year-buckets) and it changes nothing: the delta is inside the A-1b 8pp band, sign-incoherent with the n5 half, and -32.70pp under SPY. Recorded as an in-band fact, not evidence. Unknowns = 3; slot_fill 0.992; days_uncovered 3844 vs 4073. The n20 book remains what batch 2 certified: the strategy has no width in this family regardless of sort key.
"""
note = """
Batch 5, Arm 1 (owner-mandated 2026-08-22). Second width (n20, 400k) of
the registered pair with iv-ranker-live (n5) -- the bar reads BOTH widths
together. Zero engine build: rank_by="iv_rank" already exists
(portfolio.py:313/780, validated :1056, fail-loud on missing IV history).
Supersedes the archive card variants/_archive/iv-ranker.toml, which is
smoke-tier (tier="full", n_slots=1, no engine sha, no data hash,
is_evidence=false) and unusable.

Bar, as registered in the proposal (verbatim, both widths read together):

MECHANISM (or the run is not evidence): `entry_ranked_iv_unknown` must be a
small share of ranked candidates -- if most names rank NEUTRAL 0.5 the sort
degenerated. Traded book must differ materially from frozen's. Card reports
campaigns, days_uncovered, assignment_rate, slot occupancy / idle slot-days
(the 2026-08-05 starvation diagnostics, first measurement at depth 531).

SHIP BAR (resolves the 2026-08-08 ruling): total return ≥ SPY +37.34% at
n5, not one-year-carried, n20 within 8pp of frozen n20. Anything less ⇒
KILL: `rank_by="iv_rank"` archived rejected, vol_pctile stands as evidenced
default.

SECONDARY (exploration fact, not a ship claim): materially better than
frozen outside both noise bands, year-bucket coherent, A-5-surviving ⇒
"iv_rank carries selection signal beyond vol_pctile" -- feeds a Batch-6
proposal at the owner's gate. Within bands or one width only ⇒ not
evidence; kill stands.

Registered prediction (proposal, section 1): fails ship bar ~95%; vs frozen
uncertain (starvation term unmeasured at this depth) -- predicted inside
the 15pp band at n5, campaigns < 142, Sharpe ≤ frozen, n20 within 8pp.
Predicted outcome: ruling resolves to KILL; informative residue =
starvation-at-depth measurement.

Pre-flight IV coverage check (Builder, 2026-08-22, this run): data/live/grid_iv/d0.3_dte11.parquet
(matches frozen's put_delta=0.3/target_dte=11) covers 534 of the 547-symbol
universe.py regex match; 13 tickers (EWZ, SIRI, PLUG, CHPT, GPRO, PARA, AMCR,
DD, FLEX, FTV, DOC, NVR, WBA) have zero IV history rows in the 2024-08-01 ->
2026-07-01 window and will rank NEUTRAL 0.5 -- a small share (~2.4%) of the
pool, not a degenerate sort. Verified, not assumed.

Source: vault/10 Live Paper Bot/2026-08-22 -- Batch 5 proposal, selection metric.md, section 1.
"""

[ops]
n_slots = 20
capital = 400000.0

[knobs.rank_by]
value = "iv_rank"
why = """
Rank the candidate pool by IV RANK (12-month percentile of implied vol)
instead of realized-vol percentile. Never graded properly: the only prior
card is smoke-tier (variants/_archive/iv-ranker.toml, tier="full", n_slots=1,
no engine sha, no data hash, is_evidence=false). Real prior evidence
(2026-08-05, 154-name pool): lost at n3/n5 via self-starvation (idle
slot-days 452 vs 38), won total-return at n1 -- and the 2026-08-06
correction noted the starvation penalty is pool-depth dependent and
unmeasured at 531 names (overlap 140; a 3.4x deeper pool should weaken it
substantially). This run is the first measurement at that depth and
resolves the stale 2026-08-08 "ship it" ruling either way.
"""
source = "Batch 5 proposal (2026-08-22), Arm 1, owner-mandated"
