name = "rank-none-control-n20"
question = "The rank-none inertness control at the second width (n20, 400k) -- the bar requires BOTH widths to agree before ranking is certified inert."
base = "frozen"
status = "rejected"
created = "2026-08-18"
updated = "2026-08-18"
touches = ["entry"]
verdict = """
REJECTED-AS-INERT-CANDIDATE 2026-08-18 (Analyzer, batch 4; second width of
the registered pair). Bar required <=8pp gap at n20 to certify vol_pctile
inert. Actual: -8.65% vs frozen +0.19%, an 8.84pp gap -- outside the band
(narrowly at this width, decisively at n5: rank-none-control, 21.15pp gap).
MATERIALLY WORSE AT BOTH WIDTHS per the pre-registered joint bar: vol_pctile
carries real selection signal.

MECHANISM DIFFERS BY WIDTH, which itself is evidence the effect is real
selection quality and not a single fill-order artifact: at n5 the channel is
assignment avoidance (assignment_rate 0.197->0.329, days_uncovered
641->1034, campaigns 142->79). At n20 assignment_rate actually falls
slightly (0.249->0.225) and campaigns rise (377->400, slot_fill_pct ~0.99
both arms -- no starvation at either width, pool_depth_mean ~7.9 both) --
so the loss at this width is not assignment-driven, it is lower P&L per
completed campaign among the extra names universe-order pulls into the
deeper 20-slot book. Two different mechanisms, same direction, same
magnitude class: consistent with vol_pctile encoding real option/stock
quality information across the pool, not just an assignment-avoidance
heuristic that happens to help the top 5.

Read together with rank-none-control and prefer-slice-6-10: campaign
certification does NOT assemble on the "ordering carries nothing" branch.
See [[2026-08-18 — Batch 4, the ranking is real, campaign certified with a live selection axis open]].
"""
note = """
RUNS ONLY ON OWNER SIGN-OFF. Batch 4 is a PROPOSAL at the owner's gate (batch-3 terminal state: shortlist empty, campaign paused); this file is a pre-registration in the buffer-x-delta-pair pattern -- the question and bar go on the record before any run, and the run happens only if the owner opens the gate.

THE CONTROL THE SLICES COULD NOT BE. Batch-3's rank slices confounded ordering with starvation (a 5-name window against an 11.7-deep pool, empty 86-229 days). This arm deletes the vol_pctile SORT instead: gate-passers fill slots in fixed universe order, every candidate stays in the pool, nothing ever starves. Whatever it measures is ordering signal alone. Paired file for the second width (card filename is knobs-hash only; one fresh card per variant).

THE BAR, REGISTERED NOW (both controls read together): if rank-none-control lands within noise of frozen at BOTH widths (<=15pp at n5, <=8pp at n20), the vol_pctile ranking is certified INERT -- selection closes and the full campaign certification assembles (Batch 1: exit closed; Batch 2: spine family under SPY; Batch 3: slices starvation-confounded; Batch 4: ordering itself carries nothing). If it is MATERIALLY WORSE at both widths, vol_pctile has real signal and the batch-3 slice results were starvation artifacts on top of a live axis. The preference-form arm separates the remaining ambiguity: it re-orders without ever starving, so any gap it shows IS ordering signal, clean of the window_empty confound (86-229 starved days on the batch-3 slices).

KNOBS, IN PROSE (nothing is built; the schema cannot express this yet): requires one closed-set value on an existing knob -- rank_by = "none" (fill in universe tie-order among gate-passers; the sort key becomes a constant so the fixed tie order decides). One engine line plus validation plus tests, built ONLY if the owner opens the gate. No threshold, no window, no new mechanism -- the deletion of one.
"""

[ops]
# Width pair: 20 slots at $20k/slot (capacity-n20 pattern).
n_slots = 20
capital = 400000.0

[knobs.rank_by]
value = "none"
why = """
The measurement instrument, not a strategy: delete the vol_pctile sort -- the
key becomes a constant and gate-passers fill slots in the FIXED universe order
(the pool's tie-order, market.universe.index). Every candidate stays in the
pool, nothing can starve, so any gap against frozen is ordering signal alone.
Closed-set value on the existing knob, refused elsewhere. Built and tested
2026-08-18 (BUILD A batch 4, test_rank_none.py, 5 cases). Second width of the
registered pair: the bar reads BOTH widths together.
"""
source = "Batch-4 pre-registration (batch-3 close, 2026-08-18); owner gate opened 2026-08-18"
