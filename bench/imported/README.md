# Imported studies

Pre-bench runs (2026-07 to 2026-08) copied in as raw JSON so the variants' `[[evidence]]` blocks can cite them. None carries an engine sha, data fingerprint or config hash, so `bench promote` does not accept them as evidence.
